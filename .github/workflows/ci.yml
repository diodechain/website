name: "CI"
on: ["push", "pull_request"]

jobs:
  install_test:
    name: "Install and run app"
    strategy:
      matrix:
        os: ["windows-latest", "ubuntu-latest", "macOS-latest"]
        go: ["1.14.x"]
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v2
    - run: |
        cat install.sh | sh -s v0.3.3
        DIR=~/opt/diode
        echo "::add-path::$DIR"
      shell: bash
    - run: diode config -list
      shell: bash
